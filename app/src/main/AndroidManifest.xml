<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rynkbit.dienstplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Workplan"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".soldiers.SoldierActivity" />
        <activity android:name=".soldiers.LikesPostActivity" />
        <activity android:name=".soldiers.SoldierConnectionActivity" />
        <activity android:name=".post.PostActivity" />
        <activity
            android:name=".workplan.group.CreateGroupsActivity"
            android:label="@string/title_activity_create_groups"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".workplan.task.CreateTaskActivity"
            android:label="@string/title_activity_create_task"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>